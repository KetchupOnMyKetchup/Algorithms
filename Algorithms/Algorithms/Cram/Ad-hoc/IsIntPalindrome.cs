using System;
using System.Collections.Generic;
using System.Text;

namespace Algorithms.Algorithms.Cram.Ad_hoc
{
    class IsIntPalindrome
    {
        //public static bool IsPalindrome(int x)
        //{
        //    if (x < 0) return false;

        //    int original = x;
        //    int rev = 0;
        //    while (x != 0)
        //    {
        //        int pop = x % 10;
        //        x /= 10;
        //        if (rev > int.MaxValue / 10 || (rev == int.MaxValue / 10 && pop > 7)) rev = 0;
        //        if (rev < int.MinValue / 10 || (rev == int.MinValue / 10 && pop < -8)) rev = 0;
        //        rev = rev * 10 + pop;
        //    }

        //    if (original == rev) return true;

        //    return false;
        //}
    }
}
